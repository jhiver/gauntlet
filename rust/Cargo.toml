[package]
name = "gauntlet"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = "0.4"
regex = "1.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
toml = "0.8"
once_cell = "1.20"

[dev-dependencies]
tempfile = "3.10"
